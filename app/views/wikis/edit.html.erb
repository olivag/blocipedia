<%= form_for @wiki do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body, rows: 8 %>

<!--   <%= f.label :private, class: 'checkbox' do %>
    <%= f.check_box :private %> Private Topic
  <% end %> -->
  
  <%= f.submit "Submit" %>
<% end %>